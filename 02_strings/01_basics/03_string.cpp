#include <iostream>
#include <string>
using namespace std;

// but in string it doesn't require [] brackets and you don't have to perform 'for loop' to taken out lenth
// once you initialize the input in 'char' then it can't change but in string you can change

int main(){
    string str = "This is where i want to live";
    cout<<str<<endl;
    return 0;
}